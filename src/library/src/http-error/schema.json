{
  "$schema": "http://json-schema.org/schema",
  "$id": "PromptKitHttpErrorSchema",
  "title": "Add the http-error utility",
  "type": "object",
  "properties": {
    "project": {
      "type": "string",
      "description": "The Angular project to add the utility to.",
      "$default": {
        "$source": "projectName"
      }
    },
    "path": {
      "type": "string",
      "format": "path",
      "description": "Path where the utility is generated. Defaults to libs/prompt-kit."
    }
  },
  "required": []
}
